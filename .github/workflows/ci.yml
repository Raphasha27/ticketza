name: Continuous Integration

on:
  workflow_dispatch:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  frontend: 
    name: Frontend (Next.js)
    runs-on: run: working-directory: ./frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: cache-dependency-path: './frontend/package-lock.json'
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
        env: 
    NEXT_TELEMETRY_DISABLED: 1

  backend: 
    name: Backend (Python)
    runs-on: run: working-directory: ./backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: Lint
        run: python -m py_compile $(find . -name "*.py" | head -20) || true

  mobile: 
    name: Mobile (Expo)
    runs-on: run: working-directory: ./mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: cache-dependency-path: './mobile/package-lock.json'
      - name: Install dependencies
        run: npm install
      - name: Type Check
        run: npx tsc --noEmit

  security_scan: 
    name: Security Scan (Trivy)
    needs: [frontend, backend, mobile]
    runs-on: - uses: actions/checkout@v4
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          ignore-unfixed: true
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'